import React, {useContext} from 'react';
import { DataContext } from '../providers/DataProvider';

const Link = (props) => {
  const { updateLink ,deleteLink} = useContext(DataContext)

 
};

export default Link;
